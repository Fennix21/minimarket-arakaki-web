# Minimarket Arakaki WEB — Mapa del proyecto (leer antes de explorar)

Réplica del sitio de Systeme.io (www.minimarketarakaki.com) para poder optimizarlo con IA.
Sitio de la bodega + carrito → WhatsApp + CRM de WhatsApp + base de clientes. Arquitectura calcada de WHAPE.

## Stack (no explorar: es esto)
- **Vercel serverless** + Node **CommonJS**. `package.json` mínimo (ÚNICA dependencia: `web-push` para las notificaciones). SIN build de frontend. `fetch` global.
- BD = **Upstash Redis** vía REST (helper `redis(cmd)` duplicado en cada api/*.js).
- Frontend = HTML sueltos. Las 20 páginas de categoría son shells que renderizan desde `data/catalog.js` con `assets/site.js`.
- `vercel.json`: cleanUrls, maxDuration 30 para whatsapp.js y chat.js, headers de caché (7d para /img, 5min+SWR para /assets y /data).
- **Deploy = `git push` a main** (repo GitHub conectado a Vercel).
- WhatsApp del negocio: **51977737199**. Imágenes y videos YA MIGRADOS al repo (jul 2026): fotos de producto en `img/productos/<slug>/` (WebP ≤640px q80), videos en `img/videos/` (540p H.264 sin audio), beneficios/logo/redes en `img/`. Cero dependencia del CloudFront de Systeme (las URLs cloudfront que quedan en catalog-fuente.json son elementos que el build descarta).

## Archivos
| Archivo | Qué es |
|---|---|
| `index.html` | Home: preloader, portada (fachada `img/fachada-principal.webp` + video en círculo + botón que abre el POPUP del mapa), cinta marquee, tarjetas de categorías con imágenes rotando, beneficios, form Club Arakaki (→ /api/pedido registro). POPUP Fiestas Patrias: 1 vez/día (localStorage `arakaki_fp_dia`), SOLO en julio, video Machu Picchu + countdown al 28 jul |
| `<categoria>.html` ×20 | Shells generados por `tools/build-pages.js`. Llaman `renderCategoria('<slug>')` |
| `assets/site.css` | Estilos de todo el sitio público (marca: rojo #7c0f14, negro #262626, Montserrat/Poppins). Los fondos de las 5 zonas editables salen de las variables `--bg-*` del `:root` (ver receta de fondos): ahí viven los **defaults** y site.js los pisa si el dueño los cambió |
| `assets/site.js` | Preloader, header/menú/footer inyectados (el **lema del header** y **todo el footer** salen de `SITIO_DEF` + fetch a `/api/sitio`; editables desde panel → 📝 Sitio), **fondos editables** (`aplicarFondos()` pisa las variables `--bg-<zona>` de site.css con el CSS que trae `/api/sitio` en `f`; cacheados en localStorage `arakaki_fondos` y aplicados al arrancar para no parpadear), carrito (localStorage `arakaki_carrito`; dirección OBLIGATORIA con direcciones guardadas en `arakaki_dirs` ≤5 como chips reusables; avisos con `avisoCarrito()` en `#car-aviso`, no alert) → mensaje wa.me + POST /api/pedido, `renderCategoria()`, **combos de venta cruzada** (sección "El complemento perfecto" al pie de cada categoría con tarjetas premium de otras categorías — pareo automático `COMPLE_AUTO` rotando cada día u override del dueño en `/api/precios` `c` — + "el toque final" `pintarCompleCarrito()`: mini-sugerencias con ➕ dentro del carrito según el último producto elegido; `cargarVivo()` comparte UNA respuesta de /api/precios entre página y carrito; stats comple_elegir/comple_carrito), chat vendedor flotante (`#chat-fab`/`#chat-caja` → /api/chat; solo aparece si GET /api/chat da `{on:true}`; estilo mensajería: "escribiendo…" + párrafos `\n\n` revelados uno a uno + botones de respuesta rápida `sugerencias` + pantalla completa en móvil; historial en sessionStorage `arakaki_chat`). **Club Arakaki (cuenta del cliente)**: bloque "Cuenta del cliente" con sesión en localStorage `arakaki_sesion` — o sessionStorage si el cliente destilda "Mantener mi sesión iniciada" — (flags de GET /api/cuenta cacheados 5 min en sessionStorage `arakaki_club_flags`); si el Club está on añade "👤 Mi cuenta" al menú y botón al chat, pinta estrellas ⭐ (`.prod-fav`) en las cards para marcar favoritos, manda 1 visita/día (`arakaki_visita`), refleja las direcciones de la cuenta en el carrito (prefill de la principal + chips 👤 `dirsDeCuenta()`, conviven con las locales `arakaki_dirs`) y expone `renderCuenta()` (login/registro con PIN + check mantener sesión + recuperar con correo `pintarRecuperar()`, o dashboard: puntos, promos, sorteos, favoritos, "mi último pedido", ❓ mis preguntas al negocio con la respuesta del dueño, 👤 mis datos (foto de perfil comprimida en canvas a 144px, nombre, correo con nota de beneficios), 📍 direcciones de entrega principal+adicionales y 🔑 cambiar PIN). **Compartir producto**: botón 📤 en cada card (TODOS los visitantes, `pintarShareBtns`; sin estrella ocupa su lugar arriba-izquierda y con Club se corre solo a su derecha vía clase `tiene-fav`) → menú `#share-menu` con 💬 chat (wa.me con mensaje + enlace /api/compartir que pinta la tarjeta OG en WhatsApp), 📸 imagen 9:16 para estados (canvas 1080×1920 dark-gold con la foto COMPLETA a su ratio, medallón de precio y CTA verde → navigator.share o descarga) y 🔗 copiar; al llegar con `?p=` el producto brilla (`destacarCompartido`); stats compartir_chat/compartir_estado/compartir_link; copys editables en panel → 📝 Sitio → 📤 Compartir (campos comp* de config:sitio, defaults en SITIO_DEF) |
| `data/catalog.js` | Catálogo (341 productos, 20 categorías). GENERADO: no editar a mano |
| `data/catalog-fuente.json` | Datos extraídos del sitio original (fuente de verdad para regenerar) |
| `tools/build-catalog.js` | catalog-fuente.json → data/catalog.js |
| `tools/build-pages.js` | Genera las 20 páginas de categoría |
| `tools/dev-server.js` | Preview local con cleanUrls (`node tools/dev-server.js [puerto]`, def. 3210); /api/* responde stub (incluye stubs de /api/chat y /api/cuenta con perfil de prueba para ver /mi-cuenta y las estrellas en local, y de /api/precios con un agotado y un producto nuevo de muestra en /pisco) |
| `mi-cuenta.html` | Página de la cuenta del cliente (Club Arakaki): shell estándar (preloader + catalog.js + site.js) que llama `renderCuenta()` (acceso con "mantener sesión" y recuperación por correo, o dashboard con beneficios + preguntas al negocio + editar datos/foto/direcciones/PIN). `noindex`. Aparece en el menú solo si el Club está prendido |
| `panel.html` | CRM del dueño (pass = ARAKAKI_ADMIN_PASS): 🏠 Inicio (dashboard: tarjetas-resumen + últimos pedidos/chats + guía), 💬 Chats, 🛒 Pedidos, ❓ Consultas (arriba: preguntas que el chat web no pudo responder, ✓ = revisada y se borra · abajo: 💬 Preguntas del Club que los miembros dejan en /mi-cuenta, con input para responder 💾 — la respuesta se refleja en la cuenta del cliente — y 🗑 para borrar; badge = consultas + preguntas sin responder), 💰 Precios (overrides de precios en vivo + botón de disponibilidad por producto ✅ se vende → 😕 agotado → 🙈 oculto [config:stock] + ➕ subir producto nuevo con foto comprimida en canvas a ≤640px [config:prodextra + prodimg:*], con lista para editar ✏️ / quitar 🗑), 👥 Club (interruptores del Club por función + puntos por sol · editor de promos exclusivas y sorteos con 👥 participantes y 🎲 ganador al azar · tabla de clientes con puntos/gasto/última visita/🔑 tiene PIN + resetear PIN y ajustar puntos · 🗺️ Zonas: calles agrupadas bajo demanda), 📊 Analíticas, 📝 Sitio (lema del header + textos del carrito/entrega [nota "Usar mi ubicación" opcional + aviso de dirección faltante] + todos los textos del footer: títulos, dirección/referencia, horario, teléfonos, redes y copyright + 📤 copys de compartir producto (mensaje de chat, descripción del preview OG y textos de la imagen del estado) → config:sitio · 🎨 Fondos: por zona (`FONDO_ZONAS`) elegir color sólido o degradado lineal/radial de 2-3 colores con vista previa en vivo → config:fondos · 🎬 Videos de las categorías: por página elegir el video del hero [repertorio de img/videos/ hardcodeado en `VIDEOS_REPO`, URL propia https o sin video] + título/subtítulo → config:videos · 🧩 Combos (venta cruzada): interruptor GENERAL prender/apagar todo + por página elegir 🤖 automática / ✍️ yo armo el combo [título + subtítulo + hasta 8 productos con buscador y chips] / 🚫 sin sección + interruptor "el toque final" del carrito → config:comple · 🛒 Carrito "Tu pedido": apariencia del modal (textos del título/nota/toque final/botones, tamaño por elemento como escala 0.8–1.5 que se adapta sola a móvil/escritorio, color/degradado del botón Sumar y del fondo dorado, color del título, ⌨️ máquina de escribir + ✨ brillo premium) con vista previa en vivo → config:carrito, lo sirve /api/sitio como `k`), ⚙️ Bot (3 sub-pestañas: 🤖 WhatsApp = cerebro del bot + respuestas rápidas · 💬 Chat web = interruptor on/off + saludo/botones/burbuja/subtítulo del widget + cerebro PROPIO independiente de WhatsApp · 🔔 Avisos = números autorizados + notify + compositor push + 📧 Promos por correo vía Resend: asunto/título/mensaje/link, contador de miembros con correo, historial y aviso si falta RESEND_API_KEY). Menú lateral en escritorio / en móvil (≤860px): barra inferior de 5 accesos (Inicio·Chats·Pedidos·Precios·⋯Más) + menú "Más" (`#mas-panel`, resto de secciones con badge), tablas → tarjetas apiladas (`.t-cards` + `td[data-e]` pintado por JS), campos a 16px (anti-zoom iOS) y safe-areas iPhone; OJO: `#v-chats.activa` va SIN la animación `aparecer` (su translateY rompería el `position:fixed` del chat abierto). Carga /data/catalog.js para la tabla de precios. **Tema claro/oscuro**: toggle en el menú (☀️/🌙), atributo `data-theme="light"` en `<html>` que redefine las CSS vars del `:root`, persistido en localStorage `arakaki_panel_tema` y aplicado por un script temprano antes de pintar (sin parpadeo); default oscuro. **Seguro de cambios** (ver receta): popup central `#modal-fondo` (confirmar/exito/falla) + grupos `data-grupo`/`data-guardar` con estado "sin guardar" |
| `track.js` | Mini analítica (pageview + clicks) → /api/track |
| `sw.js` + `manifest.webmanifest` | Web Push + PWA: el SW muestra los push y abre la página al tocarlos; manifest con íconos `img/icon-192/512.png` y `img/apple-touch-icon.png` (gato oficial sobre #262626). En iPhone el push SOLO funciona con la web instalada (Agregar a inicio): site.js muestra la guía. El botón "🔔 Avísame de las ofertas" vive en el footer (site.js, delegación de click) |
| `api/push.js` | Web Push: GET ?key (clave pública VAPID, caché 5min) · POST subscribe/unsubscribe (rol `clientes` público, `duenos` con pass) · send/test/count/savepromo/delpromo (admin, pass). `send` acepta `image` (banner; Chrome/Edge/Android, iPhone/Firefox la ignoran), registra la campaña en `pushlog` con id `c<ts36>` para contar clics · `test` con campos = ensayo de la promo real solo a los dueños · `count` devuelve TODO el panel (contadores + promos guardadas + historial con clics). Compositor del panel (⚙️ Bot → 🔔 Avisos): promos guardadas reutilizables (no se borran al enviar), buscador de fotos del catálogo, vista previa, contadores de caracteres, aviso anti-saturación (<24h) y caja de estrategias. Tope 5000 suscriptores por rol |
| `api/_push.js` | Motor compartido (lib `web-push` + VAPID): `pushTo(rol,payload)` con poda de suscripciones muertas (404/410) y `pushDuenos(titulo,cuerpo)` — enganchado DENTRO de `notifyOwner` de pedido.js/chat.js/whatsapp.js (push gratis además del WhatsApp, que cobrará por mensaje desde oct-2026; respeta config:notify) |
| `api/_correo.js` | Motor de correos vía **Resend** (REST con fetch, sin dependencias). `HAS_CORREO` (=hay RESEND_API_KEY; sin ella todo degrada), `enviarCorreo(para,asunto,html)`, `enviarLote(items)` (batch ≤100 con pausa por rate limit) y plantillas dark-gold inline: `htmlCodigo` (código de recuperación), `htmlAvisoPin` (seguridad) y `htmlPromo` (marketing, con enlace de baja). Remitente = RESEND_FROM; ⚠️ el de prueba onboarding@resend.dev solo entrega al dueño de la cuenta Resend (verificar dominio con SPF/DKIM para escribir a clientes). Gratis: 100/día, 3000/mes |
| `api/correo.js` | GET público `?baja=<token>` — baja de correos promocionales (enlace del pie de cada promo): resuelve `baja:<tok>`→key, marca `cliente.correoBaja` y muestra página HTML de confirmación. Solo apaga promos; los correos de cuenta siguen |
| `api/compartir.js` | GET público `?p=<nombre exacto>` — enlace para compartir UN producto: página con Open Graph (foto+nombre+precio → tarjeta de preview en WhatsApp) que redirige al producto en su categoría con `?p=` (site.js lo hace brillar y le hace scroll). Funciona sin env vars (usa _catalogo.js); con Redis resuelve además config:prodextra, el precio vivo de config:precios y la descripción editable del preview (config:sitio.compOg). Caché CDN 60s |
| `api/whatsapp.js` | Webhook Meta (GET verify, POST). Si escribe un NÚMERO AUTORIZADO (config:ownerphone, lista separada por comas) → asistente ADMIN de precios (Claude + tools buscar/cambiar/quitar precio sobre config:precios; sin API key hay comando fijo `precio <prod> [monto]`). Resto: idempotencia msg.id → guarda lead → autoStatus → notifica dueño → si paused NO responde → bot Claude (getPrompt de Redis) |
| `api/precios.js` | GET público: catálogo EN VIVO `{p,s,x,v,c}` = precios (config:precios) + stock agotado/oculto (config:stock) + productos nuevos del panel (config:prodextra) + videos del hero (config:videos) + combos de venta cruzada (config:comple), caché CDN 60s. `?img=<id>` sirve la foto de un producto subido (prodimg:<id>, caché inmutable). `?vid=<id>` sirve un video subido desde el panel (ensambla los trozos vidext:*, caché inmutable + soporte de Range porque iPhone/Safari lo exige). Lo consume site.js al renderizar categorías: pisa precios, apaga tarjetas agotadas (sello dorado AGOTADO, sin poder elegirlas), esconde ocultas, suma los productos nuevos a su sección con la misma tarjeta premium y aplica el video/título/subtítulo del hero si el dueño los cambió |
| `api/sitio.js` | GET público `{s,f,k}` con caché CDN 60s (un MGET): `s` = textos editables del header (lema) y del footer (config:sitio) · `f` = fondos de las secciones y las tarjetas (config:fondos → `{clave: css}`, solo el CSS ya armado) · `k` = apariencia del carrito "Tu pedido" (config:carrito → textos, tamaños, `fx`, colores ya armados en css). site.js los aplica sobre sus defaults (funciona sin backend) |
| `api/cuenta.js` | Cuenta del cliente (Club Arakaki). GET sin token → `{on,funciones}` (config:club, caché CDN 60s; site.js decide si muestra "Mi cuenta"); GET ?token → perfil completo no-store (puntos, favs con precio vigente, `habitual` = ÚLTIMO pedido con cantidades (cliente.ultimoItems; clientes viejos: última tanda del consumo), promos vigentes, sorteos + participando, y además email/foto/direccion/direcciones + `preguntas` = las suyas de la lista global con respuesta). POST: `crear` (celular+PIN 4-6 díg., hash **scrypt+salt** en cliente:<tel>.pinHash/pinSalt; si reclama un cliente histórico avisa al dueño) · `entrar` (error genérico, freno `pinrl:*` 5/h por tel y 20/h por IP) · `recuperar` (celular + correo registrado; error genérico, mismo freno; con Resend manda código de 6 díg. al correo `reccode:<tel>` TTL 15min y responde `{codigo:true}`, sin Resend pide también el PIN nuevo y cambia directo cerrando TODAS las sesiones) · `reccode` (paso 2: canjea el código por el PIN nuevo, máx 5 intentos `reccodetry:*`, cierra sesiones, avisa al dueño + correo de seguridad, entra de una) · `salir` · `fav` (toggle cliente.favs, valida contra _catalogo o config:prodextra) · `sorteo` (ZADD NX sorteo:<id>) · `visita` (1/día Lima → cliente.visitas/ultimaVisita; funciona con token o solo uid) · `perfil` (nombre+email; email vacío quita la recuperación) · `foto` (dataURL jpeg/png/webp ≤90k chars → cliente.foto; vacío la borra) · `dirs` (direccion principal + direcciones[≤5] adicionales) · `pin` (pinActual+pinNuevo, cierra las demás sesiones) · `pregunta` (≤400 chars, máx 5/día `pregrl:*` → LPUSH `preguntas` + aviso al dueño). Sesión = `sess:<token>` TTL 180d, anotada en cliente.sess[≤5] para poder cerrarlas al resetear PIN. Cada acción respeta su interruptor de config:club; sin Redis todo responde `{on:false}` |
| `api/chat.js` | Chat CAPTADOR de suscriptores de la web (widget de site.js) — **NO vende**. GET → `{on,saludo?,botones?,invitacion?,subtitulo?}` (textos editables de config:webchatui; si faltan, site.js usa sus defaults); POST {sid,uid,mensajes} → `{reply,sugerencias,push}` (el bot termina con línea `>>> a | b | c` que extraerSugerencias convierte en botones; si el bot pone una línea `[[PUSH]]`, el backend la quita y devuelve `push:true` → site.js pinta el botón "🔔 Activar avisos gratis") vía Claude con cerebro INDEPENDIENTE: `config:webprompt` si existe (panel → 💬 Chat de la web), si no la MISIÓN por defecto `MISION_CAPTADOR` (orienta breve y persuade para activar avisos push + dejar correo + WhatsApp; ya NO cae al cerebro de WhatsApp); siempre + REGLAS_WEB. Tools: `buscar_productos` (SOLO para ubicar la categoría a la que mandar al cliente — `/slug` — NO cotiza), `registrar_suscriptor` (guarda WhatsApp/correo en el Club `cliente:<key>`/`clientes` ZSET, key = WhatsApp con 51 o `e:<correo>` si solo dejó correo; +aviso a dueños; stat:chatweb_suscriptor) y `registrar_consulta` (pregunta que el bot NO pudo responder → LPUSH `consultas` + aviso a dueños, máx 3 avisos/sesión). **Recompra (única excepción al "no vende"):** si el cliente reconocido (por `uid`) pide "lo de siempre", `pedido_habitual` trae sus productos más comprados y, al confirmar, `registrar_pedido` cierra el pedido (LPUSH `pedidos` + aviso al dueño). Rate limit por sesión (40/h) e IP (120/h) en `chatrl:*`. El historial vive en el navegador, NO en Redis |
| `api/_catalogo.js` | GENERADO por build-catalog: índice liviano [{c,n,p,i}] de productos (i = ruta de la foto) para whatsapp.js/crm.js/compartir.js. No editar a mano |
| `api/crm.js` | Backend del panel: list/get/send/status/rename/note/tags/pause/clearchat/delete/pedidos/pedidoestado (al marcar **entregado** acredita puntos 1 sola vez: flag `puntosOk` en el pedido, `floor(total×puntosPorSol)` si config:club.puntos)/consultas/consultadel/preguntas/pregresp (guarda la respuesta del dueño en la lista `preguntas`; el cliente la ve en /mi-cuenta)/pregdel/correoinfo (miembros con correo sin baja + historial `correolog`)/correopromo (campaña por correo vía Resend: asunto+titulo+texto+url, genera `cliente.bajaTok`+`baja:<tok>` por destinatario, dedup por email, log y stat:correo_enviados)/clientes (incluye puntos, gastoTotal, ultimaVisita y `tienePin`; nunca manda pinHash/sess)/clientedel/stats/getprompt/setprompt/resetprompt/setnotify (owner+notify, ya NO webchat)/getwebchat/setwebchat (on/off + textos + cerebro del chat web + **ficha de datos oficiales** config:webdatos + **FAQ oficiales** config:webfaq + **temporadas/campañas** config:webtemporadas y selección config:webtemporada + mensajes de sistema apagado/limite/errtec dentro de config:webchatui; getwebchat devuelve además promptDefault=MISION_CAPTADOR y reglas=REGLAS_WEB para mostrarlas/copiarlas en el panel)/gettemplates/settemplates/getprecios (devuelve {p,s,x})/setprecio/setstock (se vende·agotado·oculto)/addprod (producto nuevo o edición con id; foto dataURL → prodimg:*)/delprod/getsitio/setsitio (lema + textos del footer)/getfondos/setfondos (fondos de las secciones y las tarjetas: recibe el modelo {t,c1,c2,c3?,a?} por zona y ARMA el CSS con `normFondo` validando #rrggbb + ángulo entero → config:fondos; los lee /api/sitio)/getvideos/setvideos (video/título/subtítulo del hero por categoría → config:videos; getvideos devuelve también `subidos`)/getcomple/setcomple (combos de venta cruzada por página: automático `{}` · personalizado `{t,s,prods[≤8]}` con nombres validados contra _catalogo+prodextra · apagado `{off:1}` + interruptores `on` general y `car` del carrito → config:comple; getcomple devuelve también `extras` para el buscador)/vidini/vidchunk/vidfin (subida de video en trozos base64 → vidext:* + config:vidsubidos; vidfin valida cabecera MP4 'ftyp')/viddel (borra trozos + índice + overrides que lo usaban)/getclub/setclub (interruptores + promos + sorteos; borrar un sorteo borra su ZSET)/resetpin (borra PIN y cierra sesiones)/setpuntos (canjes)/sorteoinfo/sorteoganador (ZRANDMEMBER)/zonas (calles agrupadas desde direcciones de clientes+pedidos) |
| `api/pedido.js` | SIN pass (solo escribe): action 'pedido' (carrito web → LPUSH pedidos; captura `telefono`/`uid`/`geo`) y 'registro' (Club → cliente:<tel>). Notifica al dueño por WhatsApp. Además construye el **archivo de consumo** (`upsertPerfil`): suma cada producto comprado a `cliente:<tel>.consumo`, incrementa `pedidos`, guarda `ultimoPedido`/`ultimoItems[≤30]` (foto del último pedido con cantidades, la usa /mi-cuenta)/`direccion`/`gastoTotal` y enlaza el dispositivo (`uid:<token>`→tel) para reconocer al cliente cuando vuelve |
| `api/perfil.js` | GET público SOLO lectura `?uid=<token>`: reconoce al cliente por su token de dispositivo (resuelve `uid:<token>`→tel→cliente:<tel>) y devuelve `{conocido,nombre,direccion,telefono,pedidos,favoritos,habitual}` (habitual = top productos por frecuencia con precio vigente, para prellenar el carrito y armar "lo de siempre"). site.js lo consume en `reconocerCliente()`; nunca acepta lookup por teléfono. Caché no-store |
| `api/track.js` | Suma contadores stat:* en Redis |
| `api/_prompt.js` | DEFAULT_PROMPT del bot vendedor. ⚠️ El prompt VIVO está en Redis `config:prompt` (panel → ⚙️ Bot) |

## Claves Redis
`lead:<phone>` {phone,name,status,paused,note,tags[],messages[≤300],lastMsgId} · `leads` ZSET · `pedidos` LIST (≤500, {id,nombre,telefono?,direccion,geo?,items,total,pagina,ts,estado}) · `cliente:<key>` {id,nombre,telefono?,email?,interes?,club,origen?,creado,actualizado, +**archivo de consumo**: pedidos,ultimoPedido,ultimoItems[≤30 {name,qty,price,img}],gastoTotal,direccion?,uids[],consumo:{"<nombre>":{veces,cant,ultima,img,price}}} (key = WhatsApp con 51, o `e:<correo>` si el suscriptor del chat solo dejó correo; el consumo lo escribe upsertPerfil de pedido.js, favoritos/recurrentes se DERIVAN ordenando por veces) · `uid:<token>` → `<tel>` (TTL 400d: token de dispositivo del cliente → su teléfono, para reconocerlo al volver sin re-pedir el celular) · `clientes` ZSET (member = key; re-puntuado con ultimoPedido) · `config:{prompt,templates,notify}` · `config:ownerphone` hasta 6 números separados por coma (TODOS reciben avisos y usan el asistente de precios; editable en panel → ⚙️ Bot) · `config:precios` JSON {"<slug>|<nombre exacto>": "85"} (overrides que PISAN los precios de catalog.js; los lee /api/precios) · `config:stock` JSON {"<slug>|<nombre exacto>": "agotado"|"oculto"} disponibilidad en vivo (ausente = se vende; agotado = tarjeta apagada con sello dorado y sin poder elegirla, oculto = no se pinta; panel → 💰 Precios, vale también para los productos de config:prodextra) · `config:prodextra` JSON array ≤80 {id,cat,sec?,nombre,precio,img,ts} productos NUEVOS subidos desde el panel sin deploy (site.js los suma a su sección de categoría con la misma tarjeta premium; nombre único global porque carrito/favs identifican por nombre; crm.js addprod/delprod) · `prodimg:<id>` dataURL JPEG ≤640px de la foto de un producto subido (la sirve GET /api/precios?img=<id> con caché inmutable; se borra junto con su producto) · `config:webchat` '1'/'0' interruptor del chat vendedor de la web (panel → ⚙️ Bot, junto a los avisos) · `config:webprompt` cerebro PROPIO del chat web (vacío = usa la misión CAPTADORA por defecto `MISION_CAPTADOR`, NO config:prompt) · `config:webchatui` JSON {saludo,botones[],invitacion,subtitulo,apagado?,limite?,errtec?} textos del widget + mensajes de sistema del chat (campo ausente = default de site.js / de api/chat.js) · `config:webdatos` texto ≤4000 (ficha de datos oficiales del negocio; se inyecta como VERDAD en el prompt del chat web, anti-invención) · `config:webfaq` JSON array ≤30 {q,r} respuestas oficiales a preguntas frecuentes (el bot contesta con esa info, no improvisa) · `config:webtemporadas` JSON array ≤12 {id,nombre,desde?,hasta?(MM-DD),saludo?,botones?,subtitulo?,invitacion?,extra?} temporadas/campañas del chat web · `config:webtemporada` '' ninguna / 'auto' por fechas (hora Lima, wrap fin de año) / '<id>' forzada — selección activa · `config:sitio` JSON {lema,visitanosTit,direccion,referencia,mapLabel,horarioTit,horario,contactoTit,telefonos,redesTit,facebook,instagram,youtube,copy,carGeoNota,carDirFalta,compChat,compOg,compLema,compCintillo,compCta,compSinPrecio} textos editables del header/footer + carrito + compartir producto (compChat = línea del mensaje de chat · compOg = descripción del preview OG, la lee api/compartir.js · compLema/compCintillo/compCta/compSinPrecio = textos de la imagen 9:16 del estado, con auto-ajuste de tamaño `encajar`) (carGeoNota = nota bajo "Usar mi ubicación" aclarando que el GPS es opcional; carDirFalta = aviso rojo cuando falta la dirección) (campo ausente = default de `SITIO_DEF` en site.js; lo sirve /api/sitio) · `config:fondos` JSON {"<zona>": {t:'solido'|'lineal'|'radial',c1,c2?,c3?,a?,css}} fondos editables de las 5 zonas del sitio (zonas = `pagina`,`vino`,`roja`,`premium`,`card`; cada una pisa la variable CSS `--bg-<zona>` del `:root` de site.css). Guarda el MODELO (lo repinta el panel) + el `css` ya armado (lo aplica site.js); zona ausente = fondo por defecto del CSS. El CSS lo arma `normFondo` de crm.js desde #rrggbb + ángulo entero: seguro por construcción. Panel → 📝 Sitio → 🎨 Fondos, lo sirve /api/sitio como `f` · `config:carrito` JSON {txt,tam,fx,toqueBg,toqueTitCol,btnSumar,btnSumarTxt} apariencia editable del modal "Tu pedido": textos, tamaños (escala 0.8–1.5 → variables `--car-esc-*` del `#carrito-modal`), colores/degradados (los arma `normFondo`, guarda modelo+css; site.js pisa `--car-toque-bg/--car-toque-titcol/--car-btn-sumar/--car-btn-sumar-txt`) y efectos (`fx.typing` máquina de escribir + `fx.brillo` brillo premium con `fx.brilloSeg` = segundos por vuelta 2–15, default 5, → variable `--car-brillo-dur`; `fx.brilloCol` = color del barrido, default dorado #d4a941, site.js arma el degradado con `brilloGrad()` → variable `--car-brillo-grad`). Panel → 📝 Sitio → 🛒 Carrito; lo sirve /api/sitio como `k` (aplanando colores a css) y lo aplica `aplicarCarrito()` de site.js (cacheado en localStorage `arakaki_carrito_cfg`) · `sess:<token>` → `<tel>` (TTL 180d: sesión del Club; los tokens vivos se anotan en cliente.sess[≤5]) · en `cliente:<tel>` además: `pinHash`/`pinSalt` (scrypt, cuenta del Club), `puntos`, `favs[]` (nombres exactos ≤60, estrella ⭐), `ultimaVisita`/`visitas` (recurrencia), `email` (recuperación de cuenta + avisos), `foto` (dataURL 144px), `direcciones[≤5]` (adicionales a `direccion`) · `config:club` JSON {login,favoritos,puntos,promos,sorteos,puntosPorSol} interruptores del Club (default TODO prendido; panel → 👥 Club) · `config:clubpromos` JSON array ≤20 {id,titulo,texto,hasta?} promos exclusivas · `config:videos` JSON {"<slug>": {v?,t?,s?}} video del hero por categoría (v = '/img/videos/x.mp4' | '/api/precios?vid=<id>' subido del panel | URL https | 'no' = sin video; t/s pisan título/subtítulo; campo ausente = default del catálogo; panel → 📝 Sitio → 🎬 Videos, lo sirve /api/precios como `v`) · `config:comple` JSON {on?,car?,cats?} combos de venta cruzada (on='0' = interruptor GENERAL apagado: ni secciones ni carrito, sin perder la config; car='0' apaga solo las sugerencias del carrito; cats = {"<slug>": {off:1} | {t?,s?,prods?[≤8 nombres exactos]}}, slug ausente = pareo automático COMPLE_AUTO de site.js; panel → 📝 Sitio → 🧩 Combos, lo sirve /api/precios como `c`) · `config:vidsubidos` JSON array ≤10 {id,nombre,n,size,ts} índice de videos subidos desde el panel + `vidext:<id>:<i>` trozos base64 ≤512KB binario (≤7 por video = tope 3.5MB; TTL 1h hasta que vidfin los vuelve permanentes; los ensambla y sirve GET /api/precios?vid=<id> con Range) · `config:sorteos` JSON array ≤10 {id,titulo,premio,hasta?,activo} · `sorteo:<id>` ZSET member=tel score=ts (participantes) · `pinrl:*` freno de intentos de PIN (TTL 2h) · en pedidos: `puntosOk`/`puntosGanados` (puntos ya acreditados al entregar) · `chatrl:*` rate limit del chat web (TTL 2h) · `consultas` LIST (≤200, {id,pregunta,producto,sid,ts}: preguntas que el chat web no pudo responder) · `preguntas` LIST (≤200, {id,tel,nombre,pregunta,ts,respuesta?,respTs?}: preguntas del Club desde /mi-cuenta; el dueño responde en panel → ❓ Consultas y el cliente la ve en su cuenta) · `pregrl:*` freno de preguntas del Club (5/día por tel, TTL 25h) · `reccode:<tel>` código de recuperación por correo (TTL 15 min) + `reccodetry:<tel>` intentos (≤5) · `baja:<tok>` → key del cliente (TTL 400d, baja de correos promocionales; el token vive en `cliente.bajaTok`) · en `cliente:<key>` además: `correoBaja` (no recibir promos por correo) · `correolog` LIST (≤30 campañas de correo {id,ts,asunto,enviados,total}) · stat:correo_enviados · `stat:*` (analítica; el chat suma stat:chatweb_msg, stat:chatweb_suscriptor y stat:chatweb_consulta; los broadcast suman stat:push_enviados) · `push:clientes` / `push:duenos` HASH {endpoint → subscriptionJSON} suscripciones Web Push (panel → ⚙️ Bot → 🔔 Avisos) · `config:pushpromos` JSON array (≤20 promociones guardadas {id,nombre,title,body,url,image,ts}) · `pushlog` LIST (≤50 campañas enviadas {id,ts,title,enviados,img}; clics en `stat:evp:push_click:/<id>`, los suma sw.js vía /api/track) · `pushimg:<id>` dataURL JPEG del banner subido desde el panel (≤30, índice `pushimgs` ZSET; lo sirve GET /api/push?img=<id> con caché inmutable; el panel lo comprime a ≤1200px antes de subir)

Estados de lead: nuevo → interesado → pedido → entregado (o descartado). autoStatus solo avanza.

## Env vars (Vercel)
WHATSAPP_TOKEN / WHATSAPP_PHONE_NUMBER_ID / WHATSAPP_VERIFY_TOKEN · ANTHROPIC_API_KEY · UPSTASH_REDIS_REST_URL/TOKEN (o KV_REST_API_*) · ARAKAKI_ADMIN_PASS · ARAKAKI_OWNER_PHONE · ARAKAKI_BOT_MODEL (def. claude-haiku-4-5-20251001) · ARAKAKI_BOT_PROMPT (opcional) · VAPID_PUBLIC_KEY / VAPID_PRIVATE_KEY / VAPID_SUBJECT (Web Push; sin ellas el sitio funciona y el botón de ofertas dice "muy pronto") · RESEND_API_KEY / RESEND_FROM (correos vía Resend: recuperación con código + promos por correo; sin la key todo degrada — la recuperación vuelve al modo directo y el panel avisa)

## Ahorro de tokens (leer SIEMPRE)
- **NO leer completos** `data/catalog.js` ni `data/catalog-fuente.json` (miles de líneas de datos): usar Grep por nombre de producto, o Read con offset/limit.
- Las 20 páginas `<categoria>.html` son **idénticas** salvo el slug: leer UNA (ej. vinos.html) basta para conocerlas todas.
- Este CLAUDE.md ya describe toda la arquitectura: no explorar con Glob/Grep para "entender el proyecto"; ir directo al archivo que indica la tabla.
- panel.html es grande (~900 líneas): leer solo la sección relevante (login / tabs / inicio / chats / precios / pedidos / club / stats / bot están en bloques marcados con comentarios `---------`).
- No usar Agent/subagentes aquí: el proyecto es chico, Grep+Read directo siempre alcanza.

## Recetas rápidas (hacer esto, sin explorar)
- **Cambiar precio** → el dueño lo hace SOLO desde panel → 💰 Precios o por WhatsApp (override en Redis, sin deploy). En código: editar `data/catalog-fuente.json` → `node tools/build-catalog.js` → commit; OJO: un override en `config:precios` pisa lo que diga el catálogo (consolidar con `node tools/aplicar-precios.js [--borrar]`, necesita las env de Upstash).
- **Cambiar producto (nombre/foto)** → Grep del nombre en `data/catalog-fuente.json` → editar → `node tools/build-catalog.js` → commit.
- **Producto nuevo SIN deploy** → el dueño lo hace SOLO desde panel → 💰 Precios → ➕ Subir producto nuevo (categoría+sección+nombre+precio+foto; vive en Redis config:prodextra y sale en su página con la misma tarjeta premium). Para volverlo permanente en el repo: seguir la receta de abajo y luego borrarlo del panel (mismo nombre = el de Redis se descarta al pintar).
- **Producto agotado/oculto** (sin deploy) → panel → 💰 Precios, columna "En la web": botón que rota ✅ se vende → 😕 agotado (visible con sello AGOTADO, estrategia de escasez) → 🙈 oculto (override en Redis config:stock).
- **Cambiar el video o los textos del hero de una categoría** (sin deploy) → el dueño lo hace SOLO desde panel → 📝 Sitio → 🎬 Videos (override en Redis config:videos, lo sirve /api/precios y lo aplica site.js). Video NUEVO sin deploy = 📤 Subir un video en ese mismo bloque: el panel lo COMPRIME en el navegador (canvas+MediaRecorder → ≤540p H.264 sin audio ~1.1Mbps, como los del repo; Firefox no sabe → sube el .mp4 original si pesa ≤3.5MB) y lo manda a Redis en trozos de 512KB (tope 3.5MB y 10 videos). Para volverlo permanente del repo: guardarlo en `img/videos/` + añadirlo a `VIDEOS_REPO` en panel.html + deploy y borrarlo del panel. El hero comparte el fondo premium dark-gold de las secciones de productos (`.hero` en site.css).
- **Producto nuevo (permanente, en código)** → convertir su foto a WebP ≤640px (q80) y guardarla en `img/productos/<slug>/` → añadirlo al array de su página en catalog-fuente.json con `img` = ruta local (`products` si la categoría tiene precios; el stream img+txt si no) → regenerar. NO hotlinkear imágenes externas.
- **Categoría nueva** → slug en `tools/build-pages.js` (PAGES) + `tools/build-catalog.js` (META) + `assets/site.js` (MENU) → correr ambos tools.
- **Cambiar textos/estilo del sitio** → `assets/site.css` y `assets/site.js` (header/menú/footer/carrito se inyectan desde ahí, NO están en los HTML).
- **Editar el lema del header o los textos del footer** (sin deploy) → el dueño lo hace SOLO desde panel → 📝 Sitio (override en Redis `config:sitio`, lo sirve /api/sitio). En código, los defaults viven en la constante `SITIO_DEF` de `assets/site.js`.
- **Cambiar el fondo de las secciones o de las tarjetas** (sin deploy) → el dueño lo hace SOLO desde panel → 📝 Sitio → 🎨 Fondos: por zona elige color sólido o degradado (lineal con ángulo / radial, 2 o 3 colores) con vista previa en vivo; "El de siempre" = sin override (override en Redis `config:fondos`, lo sirve /api/sitio como `f` y lo aplica `aplicarFondos()` de site.js pisando `--bg-<zona>`; cachea en localStorage `arakaki_fondos` para no parpadear). En código, los **defaults son las variables `--bg-*` del `:root` de site.css** (`--bg-pagina` body · `--bg-vino` franjas del home · `--bg-roja` franja del Club · `--bg-premium` `.seccion.premium` + `.hero` · `--bg-card` `.prod` + `.cuenta-card`): cambiar ahí = cambiar el default de la marca. ⚠️ Zona nueva = declararla en los TRES lados: `FONDO_CLAVES` de api/crm.js, `FONDO_CLAVES` de assets/site.js y `FONDO_ZONAS` de panel.html (+ su `--bg-<zona>` en site.css).
- **Cambiar la apariencia del carrito "Tu pedido"** (sin deploy) → el dueño lo hace SOLO desde panel → 📝 Sitio → 🛒 Carrito: textos (título/nota/toque final/botón Sumar/botón enviar/botón flotante), tamaño por elemento (escala 0.8–1.5; se adapta sola a móvil y escritorio porque el modal es una tarjeta de ancho fijo y el px base va en `calc()`), color/degradado del botón Sumar y del fondo dorado del "toque final", color del título, y efectos ⌨️ máquina de escribir + ✨ brillo premium (el mismo `.cab::after` del header, con velocidad editable en segundos por vuelta [default 5s → `--car-brillo-dur`] y color editable [default dorado #d4a941 para que resalte sobre el fondo crema → `--car-brillo-grad`]), todo con vista previa en vivo (override en Redis `config:carrito`; crm.js getcarrito/setcarrito valida con `normFondo`; /api/sitio lo manda como `k`; site.js lo aplica en `aplicarCarrito()` pisando las variables `--car-*` de `#carrito-modal` en site.css + los textos `CAR_TXT` + los efectos `CAR_FX`; cachea en localStorage `arakaki_carrito_cfg`). Los **defaults** viven en `CAR_DEF` de site.js y en las variables `--car-*` del `#carrito-modal` de site.css. ⚠️ Campo nuevo = declararlo en crm.js (setcarrito), /api/sitio (aplanado de `k`), site.js (`aplicarCarrito`) y panel.html (estado `CARED` + editor + vista previa `pintarCarPrev`).
- **Seguro de cambios del panel** (todo en `panel.html`, cero backend) → 3 piezas:
  1. **Popup central** `#modal-fondo` + `modal(o)`: `confirmar({titulo,texto,ok,no,tipo},cb)` pregunta antes de una acción (default `tipo:'peligro'` = rojo, foco en Cancelar y Enter NO confirma; `tipo:''` = pregunta normal dorada), `exito(texto)` es la confirmación verde al centro (autocierre 2.5s) y `falla(texto)` el error. **No queda ni un `confirm()` nativo**: borrar/enviar/restaurar pasan por `confirmar`.
  2. **Grupos de guardado**: la constante `GRUPOS` (club·sitio·fondos·videos·comple·carrito·webchat·prompt·tpls·notify) mapea clave → {nombre, vista, aviso, fn, nota}. En el HTML, `data-grupo="<clave>"` marca las cajas de esa sección y `data-guardar="<clave>"` su botón 💾 (que llama `pedirGuardar('<clave>')` → confirma → corre `GRUPOS[x].fn`). `data-sin-seguro` excluye lo que no es configuración (probar el chat, subir video, participantes de sorteo).
  3. **Estado "sin guardar"**: `ojoSucio` (delegado en document, input+change) marca `SUCIO[clave]` al tocar cualquier campo de un `[data-grupo]` → el 💾 late, sale el chip "● Sin guardar" y la pestaña marca ●. `verVista` avisa antes de salir de una sección con cambios pendientes (al entrar se recargan del servidor y se perderían) y `beforeunload` antes de cerrar. Cada `guardar*` cierra el ciclo con `okGuardado(clave,texto)` (limpia el sucio + popup verde) o `errGuardado(clave,e)`.
  ⚠️ Sección editable nueva = declararla en `GRUPOS` + poner `data-grupo`/`data-guardar` en su HTML + terminar su `guardar*` con `okGuardado`/`errGuardado`. 💰 Precios NO usa grupos (guarda fila por fila): su seguro es el popup de `guardarPrecio`/`restaurarPrecio` que muestra el precio de antes y el de después.
- **Combos de venta cruzada (subir el ticket)** → el dueño lo hace SOLO desde panel → 📝 Sitio → 🧩 Combos: interruptor GENERAL 🧩 (apaga/prende TODO de un golpe sin perder la config) + por página 🤖 automática (pareo `COMPLE_AUTO`, rota cada día) / ✍️ armar el combo (título+subtítulo+productos con buscador) / 🚫 apagarla, + interruptor de las sugerencias del carrito (override en Redis `config:comple`, lo sirve /api/precios como `c`). En código: la sección de la página la pinta `pintarComple()` y la del carrito `pintarCompleCarrito()` (assets/site.js); el pareo por defecto es la constante `COMPLE_AUTO` de site.js (categoría → categorías que combinan). Nunca ofrece agotados/ocultos; conversión medible en 📊 Analíticas (stat:comple_elegir y stat:comple_carrito).
- **Textos del bot de WhatsApp** → panel → ⚙️ Bot (Redis `config:prompt`); `api/_prompt.js` es solo el respaldo.
- **Ajustar el chat web** → textos (bienvenida animada, botones, burbuja, subtítulo), cerebro propio (con botón "copiar la misión por defecto" y las reglas fijas visibles en el panel; REGLAS_WEB es SOLO técnica — formato >>>, [[PUSH]], tools, anti-invención — la estrategia vive en el cerebro), **📋 datos oficiales** (verdad anti-invención, config:webdatos), **❓ FAQ oficiales** (config:webfaq, pares pregunta→respuesta), **📅 temporadas/campañas** (cada una con sus textos + instrucciones, activables a mano o **por fechas dd/mm** hora de Lima — config:webtemporadas + config:webtemporada), **✍️ mensajes de sistema** (apagado/límite/error) y **🧪 probar el chat** (mini-chat contra /api/chat): todo en panel → ⚙️ Bot → 💬 Chat de la web (cerebro vacío = usa la misión CAPTADORA por defecto, NO el de WhatsApp) · lógica/tools/límites: `api/chat.js` (reglas técnicas en REGLAS_WEB, rol por defecto en la constante MISION_CAPTADOR; el bot NO vende — capta suscriptores push/correo/WhatsApp; `getPromptWeb` inyecta datos+temporada, el GET fusiona la temporada activa sobre los textos base, `temporadaActiva`/`enRangoMMDD` con wrap de fin de año) · botón de push desde el chat + activación (`pushActivar`, `activarPushChat`) y mascota/estilos (paleta pastel oro/crema, `.chat-opt.push`): bloques "Chat vendedor" de `assets/site.js` y `site.css` · prender/apagar: panel → ⚙️ Bot. Producción = minimarket-arakaki-web.vercel.app (el dominio .com aún apunta a Systeme.io).
- **Club del cliente (cuenta/favoritos/puntos/promos/sorteos)** → prender/apagar cada función y administrar promos, sorteos, puntos y PINs: panel → 👥 Club (config:club y demás claves; los lee /api/cuenta). Frontend: bloque "Cuenta del cliente" de `assets/site.js` + `mi-cuenta.html`. Los puntos se acreditan al marcar el pedido como ENTREGADO (panel → 🛒 Pedidos).
- **Probar** → `node tools/dev-server.js` → localhost:3210 (los /api/* responden stub).
- **Publicar** → `git push` a main (Vercel redeploya solo).

## Reglas de trabajo
- **Verificar antes de commit**: `node --check` a cada .js tocado; para HTML extraer `<script>` inline y `node --check`.
- El sitio DEBE funcionar sin env vars (wa.me links puros); el CRM/bot se activa al configurarlas.
- Productos: editar `data/catalog-fuente.json` y correr `node tools/build-catalog.js` (nunca editar catalog.js a mano).
- Página nueva de categoría: añadirla a tools/build-pages.js + META de build-catalog.js + MENU de assets/site.js.
- Estilo: ES5-ish en frontend (var, funciones con nombre), español en comentarios y UI, emojis en labels.
- Popups permitidos SOLO los del dueño: mapa (botón portada) y Fiestas Patrias (1/día, julio). No añadir otros. El preloader es requisito del dueño: bloque premium **autocontenido** (id `ap-preloader`, gato de la suerte + anillos/partículas dorados, tema dark-gold) que trae su propio `<style>` y `<script>` y se auto-oculta en window.load con respaldo 5s. Vive en `index.html` y en la constante `PRELOADER` de `tools/build-pages.js` (regenera las páginas de categoría). NO usa site.css/site.js.
- Color de barra superior y acentos de marca: `--naranja: #e64831` (pedido explícito del dueño). El rojo vino `--rojo` queda para fondos de secciones.
- **Tarjetas de categoría = tema premium dark-gold** (`renderCategoria` → `.seccion.premium` + `.prod`): tarjeta negra con borde/acentos dorados `--dorado #d4a941`, nombre/precio/botón en serif Georgia, botón "Elegir producto" con degradado dorado. Estado elegido = sello dorado ✓ (`.prod-check`) + botón vino oscuro "Quitar del pedido". NO confundir con los carruseles del home (`.cat-carrusel` / `.seccion.vino`), que son otra cosa y no se tocan.
- **Imagen de producto SIEMPRE a su ratio original con `height:auto` (nunca recortar ni deformar).** El tamaño de la card se ajusta angostándola, no recortando: escritorio vía `.seccion.premium .interior` max-width (~920px, 3/fila) y móvil capando `.grilla-prods` a 300px centrada (1/fila). Así la card completa (imagen + nombre + precio + botón) entra de un vistazo sin scroll.
- **Verde = solo WhatsApp.** El botón flotante "Ver mi pedido" (`#carrito-btn`) es premium negro-oro; `--verde-wa` queda reservado para acciones reales de WhatsApp (botón de envío en el modal del carrito y FAB de chat).
